# Backend

Server-side application code lives here.

Suggested layout:
- `src/config` for environment and app configuration
- `src/controllers` for request handlers
- `src/middleware` for auth, logging, and validation
- `src/models` for domain and data models
- `src/routes` for endpoint wiring
- `src/services` for business logic
