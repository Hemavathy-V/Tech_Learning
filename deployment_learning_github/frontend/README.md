# Frontend

Client-side application code lives here.

Suggested layout:
- `src/assets` for images and static files
- `src/components` for reusable UI pieces
- `src/pages` for routed screens
- `src/styles` for global and shared styles
